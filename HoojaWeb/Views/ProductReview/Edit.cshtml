@model HoojaWeb.ViewModels.ProductReview.ProductReviewEditViewModel

<div class="card mt-3">
    <form asp-action="EditConfirm" method="post" class="mt-3">
        <div class="form-group">
            <label for="reviewId">Review ID:</label>
            <input type="text" class="form-control" id="reviewId" name="reviewId" value="@Model.ReviewId" readonly>
        </div>
        <div class="form-group">
            <label for="productId">Product:</label>
            <select class="form-select" id="productId" name="productId">
                @foreach (var item in Model.ProductsList)
                {
                    <option value="@item.ProductId" selected="@(item.ProductId == Model.FK_ProductId)">@item.ProductName</option>
                }
            </select>
        </div>
        <div class="form-group">
            <label for="review">Review:</label>
            <textarea class="form-control" id="review" name="review">@Model.Review</textarea>
        </div>
        <div class="form-group">
            <label for="rating">Rating:</label>
            <input type="number" class="form-control" id="rating" name="rating" value="@Model.Rating">
        </div>
        <div class="form-group">
            <label for="customerName">Customer Name:</label>
            <input type="text" class="form-control" id="customerName" name="customerName" value="@Model.CustomerName">
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
    </form>
</div>

